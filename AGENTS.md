If the task involves implementing the temporal gating MVP, read `docs/mvp_spec.md` before editing code.

Use `docs/mvp_spec.md` as the primary reference for intended behavior, constraints, and scope. Always verify tensor shapes, call sites, file locations, and module boundaries against the current repository state.

If the spec and the repository differ, surface the mismatch explicitly and choose the smallest reliable implementation path that preserves the MVP intent.

Do not expand beyond the documented MVP scope unless explicitly asked.
